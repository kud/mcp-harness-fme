{
  "id": "mcp-harness-fme",
  "name": "harness-fme",
  "displayName": "Harness Feature Management",
  "description": "Inspect and control Harness FME feature flags — list workspaces, browse environments, audit flag targeting rules, and kill or restore flags in a single conversation.",
  "version": "1.0.0",
  "author": "kud",
  "repository": "https://github.com/kud/mcp-harness-fme",
  "npm": "@kud/mcp-harness-fme",
  "mcpConfig": {
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@kud/mcp-harness-fme@latest"],
    "env": {
      "HARNESS_API_KEY": "${MCP_HARNESS_FME_TOKEN}"
    }
  },
  "requiredEnv": [
    {
      "name": "MCP_HARNESS_FME_TOKEN",
      "description": "Your Harness API key — generate one at app.harness.io under Account Settings → API Keys",
      "required": true
    }
  ],
  "skills": ["feature-flag-status"]
}
